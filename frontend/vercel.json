{
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/.*",
      "status": 200,
      "dest": "/"
    }
  ],
  "builds": [
    {
      "source": "/frontend/**/*",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ]
}
